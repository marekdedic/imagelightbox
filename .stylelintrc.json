{
    "extends": "stylelint-config-recommended",
    "plugins": [
        "stylelint-no-unsupported-browser-features"
    ],
    "rules": {
        "selector-id-pattern": "ilb-image",
        "plugin/no-unsupported-browser-features": [
            true,
            {
                "severity": "warning"
            }
        ]
    }
}
