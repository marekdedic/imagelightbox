sudo: required
language: node_js
dist: trusty
addons:
  chrome: stable
  apt:
    packages:
      # This is required to run new chrome on old trusty
      - libnss3
node_js:
  - "node"

script: npm test
