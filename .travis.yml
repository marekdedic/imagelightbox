language: node_js
dist: trusty
addons:
  apt:
    packages:
    # This is required to run new chrome on old trusty
  - libnss3

before_install:
  - "sysctl kernel.unprivileged_userns_clone=1"
  
node_js:
  - "8"

script: npm test
