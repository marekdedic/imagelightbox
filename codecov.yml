---
coverage:
  range: "0...100"

  status:
    project:
      default:
        target: auto
        threshold: 5%
    patch:
      default:
        target: 0%

parsers:
  gcov:
    branch_detection:
      conditional: true
      loop: true
      method: false
      macro: false

comment:
  layout: "reach,diff,flags,files"
  require_changes: false
