/* exported PreloadedVideo */

interface PreloadedVideo
{
    e: JQuery; // element
    i: string; // id
    l: boolean; // is metadata loaded
    a?: string; // autoplay
    h?: number; // height
    w?: number; // width
}
